## What's new in v0.4.3

- Add auto-instance numbering (e.g. "#2") to the Modulation Matrix and canvas node headers when multiple nodes share the same title
- Remove the comb band types from the EQ node
- Give the Comb filter node's resonance a mapping that produces an actually audible comb
- Fix the cutoff knob being greyed out on the comb filter types
- Add a loop crossfade to the Sampler so the loop wrap seam stops clicking
- Calibrate the Metallic strike to its measured peak so level stops depending on pitch, material and decay
- Normalise the Metallic modal bank so a strike peaks at its velocity
- Analog: make spread a real stereo width and detune an honest total width
- Fix the Analog sub oscillator emitting DC instead of a sub-octave tone
