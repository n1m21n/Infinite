# Whole-test-level known pre-existing failures for driver.sh on Linux.
#
# Format: TESTNAME|reason
# One line per test currently known to fail (or crash, or flake) on Linux.
# Every entry needs a one-line reason and the phase that will remove it.
# The baseline must shrink each phase and be empty of P0-P4 entries before P5 ships.

# Phase 2: Audio / MIDI
# AUDIOPARAMSWEEPTEST, AUDIORINGTEST and MIDINOTESTREAMTEST (superseded by
# INFINITE_MIDIPARSETEST, the ALSA sequencer parser fixture - see
# phase-02-audio-midi.md 2.5.2) are verified green on Linux now that
# miniaudio+ALSA are implemented, and are removed from this baseline.
# AUDIOUITEST moved to the Phase 4 section below: it drives AudioPluginNode
# (VST3 plugin hosting), not the audio device/MIDI backend this phase covers.
# AUDIOLIFECYCLETEST and AUDIORECOVERYTEST are functionally correct but flaky
# under this container's timing/scheduling jitter - see their entries below.
AUDIOLIFECYCLETEST|flaky ~40-50% under this Docker/Xvfb container: AudioEngine::Process's wall-clock kXrunGapMultiplier heuristic (src/audio/AudioEngine.cpp, shared code, not Linux-specific) trips a false-positive xrun on Stop/Start when the host is under scheduling contention. Verified functionally correct (device negotiation, restart, mailbox rate) every run; only the timing-based xrun assertion flakes. Not a Linux platform defect - owner call on whether to loosen the heuristic or accept as container-only noise.
AUDIORECOVERYTEST|flaky on the pulse backend under this container (4 of 5 direct repeated runs failed "a second flag inside the min interval adds no attempt"; null backend passed 8/8): the fixture's kAudioRecoveryMinIntervalMs=500.0 window (src/main.cpp, shared/portable) assumes a config-change recovery restart completes well under 500ms, but a real PulseAudio device negotiation under container CPU contention can itself take close to or over that window, so the second synthetic flag lands outside the interval it was meant to test. Not a Linux platform defect - same class of timing-assumption flake as AUDIOLIFECYCLETEST's xrun heuristic; owner call on whether to widen the window or make the fixture use a mocked/instant recovery path instead of a real device restart.

# Phase 3: Media / Video / Camera
# RECEXPORTTEST is implemented and passing as of this phase (was a decode-side
# livelock in MediaLinux.cpp's readahead backward-seek check comparing target
# against the decode thread's own buffer-ahead progress instead of what was
# actually delivered to the caller - fixed, see VideoHandle::deliveredSeconds).
#
# VIDEOAUDIOTEST is implemented and passing as of this phase. It was never a
# Linux platform bug: WriteFileAudioTrack loops a shorter file-audio source to
# fill the whole recorded video's duration on all three platforms (confirmed
# byte-for-byte identical intent in Platform.mm's AppendAudioUpToLocked and
# MediaWin.cpp's WriteFileAudioTrack), and AudioFileNode::loop defaults to
# true - so the test's own hardcoded `expectedDuration = 1.5` (the tone
# clip's own length) was simply wrong; fixed in src/main.cpp to compare
# against the actual recorded video duration instead.
#
# VIDEOSPEEDTEST is implemented and passing as of this phase. Root cause:
# every single-frame reverse/fast-scrub step re-seeks and pays
# av_seek_frame/avcodec_flush_buffers overhead (confirmed via instrumentation
# that a smaller GOP does not help - it made FrameUpdateCount worse despite
# shrinking the redecode distance from the nearest keyframe). Fixed by
# porting Platform.mm's existing reverse-playback frame cache design
# (kReverseLookbackSeconds / frameCache / TryUseCache) to MediaLinux.cpp's
# threaded decoder - macOS already needed this exact mechanism for smooth
# reverse playback, confirming it is a genuine cross-platform requirement
# for a software decoder without hardware seek acceleration, not a
# Linux-specific misconfiguration.

# Phase 4: VST3 / Plugins
AUDIOUITEST|stubbed: audio device and UI - depends on VST3 plugin host, not the Phase 2 audio device (P4)
PLUGINDRAGTEST|stubbed: plugin host / VST3 (P4)
# PLUGINSCANTEST (RunPluginScanTest() in src/main.cpp) only ever exercised
# Apple's AudioUnit host path (Platform::EnumerateAudioUnits against
# au:aufx:dely:appl) - it has never tested VST3 on any platform, including
# Linux, despite living in the "P4: VST3" section. AU does not exist on
# Linux at all, so this is not a stubbed-pending-Linux-work case like its
# neighbors above; it is architecturally impossible to make this pass on
# Linux without inventing an AU host, which is out of scope. VST3SCANTEST
# (below this entry until Phase 4 task 4.4, now passing and removed) is the
# real Linux VST3 scanner test this entry's old text was easy to mistake it
# for. Left in intentionally - flagging for whichever session owns the
# Phase 4 exit criteria: if "zero P0-P4 entries in this file" is a hard
# requirement, this line cannot be satisfied by more Linux work; it needs
# either an explicit exception for format-inapplicability entries, or
# RunPluginScanTest() itself to be reworked to also cover VST3 (a
# non-trivial change to already-shipped test code, not attempted here).
PLUGINSCANTEST|not a Linux gap - AU-only test (Platform::EnumerateAudioUnits), architecturally inapplicable on Linux, not "stubbed pending Linux work"

# Other pre-existing test quirks shared with macOS
GROUPTEST|"after ungroup: ... member freed" sub-check fails — pre-existing and unrelated to Linux port
ROUNDTRIPTEST|same as macOS (known-test-failures.txt): copy/paste dropped values on Predictive Modulator, Predictive Notes, and Predictive Rhythm — pre-existing and intentional per commit b4526db: CopyParams explicitly resets learned state on duplicate/copy-paste for Learn-button Predictive nodes (ResetLearnedState) so a copy starts fresh rather than inheriting the source's exact learned model. Save/load round-trips 287/287 cleanly (OK).
