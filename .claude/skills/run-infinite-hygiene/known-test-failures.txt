# Whole-test-level known pre-existing failures for driver.sh.
#
# Format: TESTNAME|reason
# One line per test currently known to fail (or crash, or flake) for a reason
# already understood and NOT caused by whatever change you're testing.
#
# driver.sh checks every failing/crashing check name against this file:
#   - listed here + still fails  -> printed as [xfail], does NOT fail the run
#   - listed here + now passes  -> printed as [stale-baseline], does NOT fail
#     the run, but tells you to delete the line (the fix landed)
#   - NOT listed here + fails   -> printed as [FAIL], DOES fail the run
#
# This means: don't re-run a test to "confirm" it's the known one — if driver.sh
# printed [xfail] for it, that IS the confirmation. Only chase a [FAIL] line.
#
# Add a line the same day you first confirm a failure is pre-existing and
# unrelated to your change (check its log against the node's actual source
# first — don't blindly baseline something that might be a real regression).
# Delete a line the same day driver.sh reports it as [stale-baseline].

PLUGINDRAGTEST|"drop onto node: id='' expected=''" — reproduces identically with Field step 18's changes stashed out; no VST plugins installed in this sandbox for the drag fixture to find, unrelated to Field UI work.
GROUPTEST|"after ungroup: ... member freed" sub-check fails — src/main.cpp (where ungroup/group logic and GROUPTEST live) has not been touched since commit 79797b8/5774620, well before this preset-content-only change (which only edited src/nodes/Field*.cpp preset arrays); pre-existing and unrelated.
DRAGTEST|"after canvas drag: node pos=(66,66) viewAnchor=(168,149) -> node MOVED, view panned : BUG" — reproduces identically on a clean rebuild with this stage's minimap-selection-outline change (src/main.cpp ~31567, drawing-only, unrelated code path) stashed out; pre-existing canvas-vs-node drag disambiguation issue, not caused by this change.
AUDIOLIFECYCLETEST|"AudioEngine::Start succeeds" fails at Platform::AudioDeviceOpen (src/audio/AudioEngine.cpp Start()) — reproduces identically on a clean rebuild with this stage's HIG-06 ImGui-styling-only main.cpp diff (PushElevatedPanelStyle/PushSliderStyle, table color theming, icon-button sweep — none of it touching src/audio/) stashed out; this sandbox has no accessible CoreAudio output device for Platform::AudioDeviceOpen to negotiate a rate with, unrelated to this change.
AUDIORECOVERYTEST|depends on AudioEngine::Start succeeding first (PollAudioRecovery drives the same Platform::AudioDeviceOpen path as AUDIOLIFECYCLETEST) — same environmental "no accessible CoreAudio device in this sandbox" cause, confirmed by the identical AUDIOLIFECYCLETEST stash-out test; unrelated to this stage's ImGui-styling-only diff.
RECEXPORTTEST|"encoder appears wedged: pending frame count never dropped below 3 for 45000ms (RecorderPump never got re-armed by AVFoundation)" — reproduces identically on a clean rebuild with this stage's Performance Matrix Edit Mode fixes (BipolarKnobFloat ItemSize, NumBox transparent frame overrides, Toggle size cap, DrumSequencer/DrawModulatorMeter theme-aware colors) stashed out; same family as AUDIOLIFECYCLETEST/AUDIORECOVERYTEST's "no accessible CoreAudio device in this sandbox" environmental limitation (movie export needs a real AVFoundation audio track to drain), unrelated to this change.
