# Chat/session-history-derived data stays on this machine - never commit or push it.
# Everything else in the brain (code/commit/doc/skill corpora, the public index,
# training datasets, router adapter) is built from what is already public in this repo.
1_extractors/output/session_history_corpus.json
1_extractors/output/antigravity_history_corpus.json
1_extractors/output/session_analysis_corpus.json
1_extractors/output/session_embeddings_cache.npy
1_extractors/output/dev_trajectory_corpus.json
1_extractors/output/knowledge_index_private.db
1_extractors/output/.sync.lock
1_extractors/output/knowledge_index_private.db-*
# L1 state (doc store, vector cache, parse cache) holds session text and its vectors.
l1/state/
# Per-case replay detail; only the scorecards in history.jsonl are kept.
5_evals/replay/runs/
