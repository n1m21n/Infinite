"""L3 of the semi-brain: networks. Typed edges from retrievable docs to the code they are
about, plus the code's own graph (defines, calls, includes) from the AST, so a query that finds
a past commit, chat or skill can follow it to files and symbols."""
